version: '3.8'

services:
  app:
    image: marley2022/ihome-agents:latest
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    working_dir: /app/research_stocks/src/research_stocks
    command: [ "python3", "main.py" ]
    restart: unless-stopped
